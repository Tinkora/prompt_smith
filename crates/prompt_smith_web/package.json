{
  "name": "@tinkora/prompt_smith_web",
  "version": "0.1.0-alpha.3",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build:wasm": "wasm-pack build --target web --release --out-dir static/pkg .",
    "prepare:wasm-smoke": "node scripts/prepare-wasm-smoke.mjs",
    "test:wasm-smoke": "npm run prepare:wasm-smoke && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "http-server": "14.1.1"
  }
}
